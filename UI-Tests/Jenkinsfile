pipeline {
  agent any
  stages{
    stage("Install dependencies") {
       steps {
            dir("UI-Tests"){
                sh 'npm ci'
            }
       }
    }
    stage("Test the UI") {
       steps {
            dir("UI-Tests"){
                sh 'HOST_IP=http://50.17.112.102/'
                sh 'TERM=dumb'
                sh 'NO_COLOR=1 npm test'
            }
       }
    }
  }
}